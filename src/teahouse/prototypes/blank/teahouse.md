# Teahouse — 实例配置

## 楼层配置

- 目标字数：约 1000 字/层

## 总结规则

- 建议总结频率：每 7 层一次
- 最大不总结层数：10 层

## 输出块 label 命名规则

- `ep{N}` — 第 N 章正文（如 `ep1`、`ep2`、`ep500`）。前端自动提取 ep 块并展示最高编号的章节
- 生成正文后必须使用 Output 工具将内容推送到前端