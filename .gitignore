# Python
__pycache__/
*.py[cod]
*.egg-info/
/dist/
/build/
# build/ 下纳入构建脚本（build_release.py / build_release.bat / README_BUILD.md），
# 仅排除机器生成的中间件与 Windows 专用大件（__pycache__ 已由上方规则覆盖）：
!/build/
/build/teahouse/
/build/mingit_tmp/

# Virtual environment
.venv/

# IDE
.vscode/
.idea/
.claude

# OS
.DS_Store
Thumbs.db

# Teahouse — autogenerated config
teahouse.yaml
teahouse.db

# Teahouse — build-time generated version inject (written by build_release.py)
src/teahouse/_version_inject.py

# Teahouse — instance data (generated at runtime)
data/
take_out/

# Node / frontend
node_modules/
dist/

# Test
tests/

# Dev-only local scripts (dev.bat / run-server-dev.sh / dev services)
ignored/

# Plugins — user-installed plugins (keep the directory and docs)
plugins/*
!plugins/.gitkeep
!plugins/README.md
CLAUDE.md
