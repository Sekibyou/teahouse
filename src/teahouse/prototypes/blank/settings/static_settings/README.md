# 静态设定（随实例入 git，不建议修改）

本目录存放**长期稳定、几乎不随剧情变化**的背景设定：时代特征、修为分段、各大势力、背景板、世界观常量等。

规则：
- **随实例 git 版本控制**（与 `../dyn_settings/` 同仓提交）。它应稳定存在。
- **不建议由导演/总结修改**。剧情推进需要更新的而是 `../dyn_settings/`（动态设定）。

需要引用本目录内容时，用占位符切片，例如 `{{settings/static_settings/时代设定.md|from="## 修为分段"}}`。
