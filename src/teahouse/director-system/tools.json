[
  {
    "name": "Read",
    "description": "读取文件内容，支持指定行范围",
    "usage": "读取文件内容，支持指定行范围。用于在操作前了解文件当前状态。\n\n参数：\n- `path`（必填）：文件路径，相对于实例根目录。例如 settings/world.yaml、floors/floor-001.md\n- `offset`（可选）：起始行号，从 1 开始。不指定则从文件开头读取\n- `limit`（可选）：最大读取行数。不指定则读取 offset 之后的所有行\n\n注意：path 必须指向文件，读取目录会报错。",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {"type": "string"},
        "offset": {"type": "integer"},
        "limit": {"type": "integer"}
      },
      "required": ["path"]
    }
  },
  {
    "name": "Write",
    "description": "写入文件内容（覆盖式），自动创建父目录",
    "usage": "写入文件内容（覆盖式）。如果文件已存在则完全覆盖，不存在则创建。会自动创建必要的父目录。\n\n参数：\n- `path`（必填）：文件路径，相对于实例根目录。例如 floors/floor-003.md\n- `content`（必填）：写入的文件内容\n\n注意：仅在需要完全重写文件时使用。增量修改请优先使用 Edit 或 WriteLine。",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {"type": "string"},
        "content": {"type": "string"}
      },
      "required": ["path", "content"]
    }
  },
  {
    "name": "Edit",
    "description": "精确字符串替换，支持全局替换",
    "usage": "对文件执行精确字符串替换。old_string 必须在文件中唯一且精确匹配（包括空白字符和换行符），否则操作失败且文件状态不变。替换后文件自动保存。\n\n参数：\n- `path`（必填）：文件路径，相对于实例根目录\n- `old_string`（必填）：被替换的精确字符串，必须完全匹配文件中的内容且唯一（除非 replace_all=true）\n- `new_string`（必填）：替换后的字符串\n- `replace_all`（可选）：是否替换所有匹配项。默认 false（只在 old_string 唯一时替换）。设为 true 则替换所有出现位置\n\n最佳实践：\n- 优先使用此工具进行增量修改，而非 Write 覆盖整个文件\n- 如果 old_string 因不够唯一而匹配失败，增加更多上下文行使其唯一",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {"type": "string"},
        "old_string": {"type": "string"},
        "new_string": {"type": "string"},
        "replace_all": {"type": "boolean"}
      },
      "required": ["path", "old_string", "new_string"]
    }
  },
  {
    "name": "WriteLine",
    "description": "替换文件中的指定行",
    "usage": "替换文件中的指定行。每次调用只能替换一行（start_line 与 end_line 相同）。如需修改多行，请多次调用。\n\n参数：\n- `path`（必填）：文件路径，相对于实例根目录\n- `start_line`（必填）：起始行号，从 1 开始\n- `end_line`（可选）：结束行号（包含）。不指定则仅替换 start_line 这一行\n- `new_content`（必填）：替换后的新行内容，\\n 会被自动处理为真实换行\n\n注意：使用前先用 Read 确认目标行号。每次只替换一行，多行修改请多次调用。",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {"type": "string"},
        "start_line": {"type": "integer"},
        "end_line": {"type": "integer"},
        "new_content": {"type": "string"}
      },
      "required": ["path", "start_line", "new_content"]
    }
  },
  {
    "name": "Glob",
    "description": "按 glob 模式匹配文件路径",
    "usage": "按 glob 模式匹配实例中的文件路径。\n\n参数：\n- `pattern`（必填）：glob 模式，相对于实例根目录。例如 **/*.md 匹配所有 markdown，floors/floor-*.md 匹配楼层文件\n\n注意：只返回匹配的文件列表，不返回文件内容。找到目标文件后用 Read 读取内容。",
    "parameters": {
      "type": "object",
      "properties": {
        "pattern": {"type": "string"}
      },
      "required": ["pattern"]
    }
  },
  {
    "name": "Grep",
    "description": "在文件内容中按正则表达式搜索",
    "usage": "按正则表达式搜索实例中的文件内容，返回匹配的文件路径和行号。\n\n参数：\n- `pattern`（必填）：正则表达式搜索模式。例如 张三 搜索包含\"张三\"的文件，log.*Error 搜索匹配模式的日志行\n\n注意：返回匹配文件列表及各文件中的匹配行数。找到匹配后用 Read 读取对应文件的具体内容。",
    "parameters": {
      "type": "object",
      "properties": {
        "pattern": {"type": "string"}
      },
      "required": ["pattern"]
    }
  },
  {
    "name": "Generate",
    "description": "调用正文模型生成内容并写入指定文件",
    "usage": "调用正文模型（writer slot）生成内容，非流式调用完成后写入指定文件。content 中可使用 {{path}} 占位符引用文件内容。\n\n参数：\n- `messages`（必填）：消息数组，每项包含 role 和 content\n- `path`（必填）：输出文件路径，相对于实例根目录。例如 floors/floor-003.md\n\n占位符语法：\n  {{path}}                                    引用整个文件\n  {{path:10-30}}                              引用文件的第 10~30 行\n  {{path|from=\"关键字\"}}                       从关键字所在行开始到文件末尾\n  {{path|to=\"关键字\"}}                         从文件开头到关键字所在行\n  {{path|from=\"A\"|to=\"B\"}}                     从 A 所在行到 B 所在行\n  {{path:10-30|from=\"A\"|to=\"B\"}}               先取行范围，再按锚点缩小\n  {{glob:模式}}                                按 glob 模式匹配多个文件，按文件名排序\n\n规则：行号范围用 : 加在文件名后，锚点修饰符用 | 分隔，锚点值是精确子串匹配（必须唯一出现一次）。不确定行号时优先使用 from= 锚点语法。",
    "parameters": {
      "type": "object",
      "properties": {
        "messages": {
          "type": "array",
          "items": {"type": "object"}
        },
        "path": {"type": "string"}
      },
      "required": ["messages", "path"]
    }
  },
  {
    "name": "SkillRead",
    "description": "读取 Skill 的完整教学内容和 SOP",
    "usage": "读取指定 Skill 的 SKILL.md，获得该 Skill 完整的方法论和 SOP。\n\n参数：\n- `name`（必填）：Skill 名称，例如 teahouse-generate-floor、teahouse-summarize\n\n注意：Skill 的名称和简介已在系统提示词中列出。当需要执行某个 Skill 定义的流程时调用此工具加载完整内容。",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {"type": "string"}
      },
      "required": ["name"]
    }
  },
  {
    "name": "GitCommit",
    "description": "执行 git 提交，锁定实例文件状态。必须指定提交类型（楼层/总结/其他）。",
    "usage": "执行 git add -A && git commit，锁定当前实例所有文件的状态。返回 commit hash 和当前分支名。\n\n参数：\n- `type`（必填）：提交类型 — \"floor\"（楼层）、\"summary\"（总结）、\"other\"（其他）\n- `number`（floor 时必填）：楼层编号，如 5\n- `start`（summary 时必填）：总结覆盖的起始楼层编号\n- `end`（summary 时必填）：总结覆盖的结束楼层编号（与 start 相同时表示单层总结）\n- `message`（必填）：提交描述（不含前缀，工具会自动拼接）\n\n调用时机：\n- 楼层完成时：type=\"floor\"，number=楼层号\n- 总结完成时：type=\"summary\"，start/end=总结范围\n- 其他变更（配置修改、资源更新等）：type=\"other\"\n\n生成的 git 消息格式：\n- floor: \"floor-5: 抵达王都\"\n- summary: \"summary-15-20: 总结十五楼到二十楼\"（start==end 时退化为 \"summary-5: 第五楼总结\"）\n- other: \"other: 初始化实例\"\n\n⚠️ 重要：调用此工具前，必须先向用户确认是否满意当前内容（如\"已完成楼层创作，请确认后提交\"）。不要自行决定提交时机。此工具调用时会触发前端确认弹窗，用户可拒绝。",
    "parameters": {
      "type": "object",
      "properties": {
        "type": {"type": "string", "enum": ["floor", "summary", "other"]},
        "number": {"type": "integer"},
        "start": {"type": "integer"},
        "end": {"type": "integer"},
        "message": {"type": "string"}
      },
      "required": ["type", "message"]
    }
  },
  {
    "name": "GitBranch",
    "description": "分支管理（列出、创建、切换、删除、重命名）",
    "usage": "分支管理操作，用于剧情分支存档、回档和实验性写作。\n\n参数：\n- `action`（必填）：操作类型\n  - `list` — 列出所有分支，* 标记当前\n  - `create` — 基于当前 HEAD 创建新分支\n  - `switch` — 切换到已有分支（会改变 floors/、variables/、settings/ 等的文件内容）\n  - `delete` — 删除分支（安全模式，未合并时拒绝）\n  - `rename` — 重命名分支（常用于将 temp-* 临时分支改名为正式名）\n- `name`（create/switch/delete/rename 时必填）：分支名\n- `new_name`（rename 时必填）：新名称",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {"type": "string", "enum": ["list", "create", "switch", "delete", "rename"]},
        "name": {"type": "string"},
        "new_name": {"type": "string"}
      },
      "required": ["action"]
    }
  },
  {
    "name": "GitCheckout",
    "description": "回到历史提交（非破坏性，创建临时分支）",
    "usage": "回到历史提交进行查看或实验。在目标提交处创建临时分支并切换过去，原分支完全不受影响。\n\n参数：\n- `target_hash`（必填）：目标提交的 hash，可从 GitLog 返回的列表中找到\n\n使用场景：\n- 回顾之前的剧情状态\n- 从历史节点开始\"如果当初…\"的实验性写作\n- 在隔离环境中安全探索不同走向\n\n注意事项：\n- 工作区内容会随之变为历史 commit 的状态（floors/、settings/、variables/ 等）\n- 所有修改都在临时分支上，不影响原分支\n- 回到原分支：使用 GitBranch switch\n- 如果在此临时分支上做了修改并提交，请务必使用 GitBranch rename 改名为有意义的名称（不要以 temp- 开头），否则切换分支时临时分支会被自动清理\n- 如果只是查看历史、没有做任何修改就切走了，临时分支会在下次切换分支时被自动删除",
    "parameters": {
      "type": "object",
      "properties": {
        "target_hash": {"type": "string"}
      },
      "required": ["target_hash"]
    }
  },
  {
    "name": "GitLog",
    "description": "查看 git 提交历史",
    "usage": "查看实例的 git 提交历史。返回最近的提交列表，包含 commit hash、作者、日期和提交信息。\n\n参数：\n- `limit`（可选）：返回的最大提交数，默认 10\n\n使用场景：回顾历史记录；查找 commit hash 供 GitCheckout 使用。",
    "parameters": {
      "type": "object",
      "properties": {
        "limit": {"type": "integer"}
      }
    }
  },
  {
    "name": "GitDiff",
    "description": "查看当前工作区与最后一次提交之间的差异",
    "usage": "执行 git diff 查看未提交的变更。返回 unified diff 格式的输出。\n\n参数：\n- `path`（可选）：指定要查看的文件路径，相对于实例根目录。不传则查看所有文件的差异\n\n使用场景：提交前确认改了什么；了解当前工作区状态。",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {"type": "string"}
      }
    }
  },
  {
    "name": "GitStatus",
    "description": "查看当前工作区文件变更状态",
    "usage": "执行 git status --porcelain 查看工作区状态。返回每个文件的变更状态（M=修改、A=新增、D=删除、?=未跟踪）和是否已暂存。\n\n无参数。\n\n使用场景：快速了解有哪些文件被修改/新增/删除，无需查看具体 diff 内容。",
    "parameters": {
      "type": "object",
      "properties": {}
    }
  },
  {
    "name": "FileOps",
    "description": "文件和目录操作：创建目录、移动/重命名、删除",
    "usage": "对实例中的文件和目录执行操作。这些操作会直接覆盖已存在的目标，请确保文件结构正确。\n\n参数：\n- `action`（必填）：操作类型\n  - `mkdir` — 创建目录（自动创建父目录，已存在不报错）\n  - `move` — 移动文件或目录（重命名/移动，目标已存在则覆盖）\n  - `delete` — 删除文件或目录（删除目录时会递归删除其所有内容）\n- `path`（必填）：目标路径，相对于实例根目录\n- `destination`（move 时必填）：目标路径，相对于实例根目录\n\n注意：\n- `mkdir` 自动创建所有必要的父目录，相当于 `mkdir -p`\n- `move` 如果目标已存在则直接覆盖（文件覆盖文件，空目录覆盖空目录，非空目录报错）\n- `delete` 删除目录时会连同内容一起删除，操作不可逆（git 可恢复）\n- ⚠️ 使用这些工具前请确认文件结构——move 和 delete 会直接修改文件系统",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {"type": "string", "enum": ["mkdir", "move", "delete"]},
        "path": {"type": "string"},
        "destination": {"type": "string"}
      },
      "required": ["action", "path"]
    }
  },
  {
    "name": "Output",
    "description": "将内容块推送到前端显示，支持 append/replace/delete 三种模式。content 支持 {{path}} 占位符引用文件内容。",
    "usage": "将内容块推送到前端显示，支持 append/replace/delete 三种模式。content 支持 {{path}} 占位符引用文件内容。\n\n参数：\n- `content`（必填）：输出内容模板，支持 {{path}} 占位符。占位符语法同 Generate 工具：\n  {{path}}                引用整个文件\n  {{path:10-30}}          行范围\n  {{path|from=\"A\"}}      锚点\n  {{glob:模式}}           glob 匹配\n- `label`（必填）：程序标识符，前端据此筛选和渲染。命名规则由 teahouse.md 约定\n- `note`（必填）：人类可读备注，如\"第一章正文第一版\"\n- `mode`（必填）：append / replace / delete\n- `target_uuid`（replace / delete 时必填）：要替换或删除的块的 uuid\n- `content_type`（可选）：内容类型，默认 \"rich_text\"\n  - `rich_text` — BBCode 富文本（默认），由宿主层解析为 HTML 后注入沙盒\n  - `text` — 纯文本，保留换行\n  - `bootstrap_js` — 沙盒基础设施脚本（全局唯一），注入沙盒 iframe 并执行\n  - `scene_js` — 场景展示脚本（替换式），由 bootstrap 调用 activateScene 触发\n  - `ui_js` — UI 组件脚本（追加式），悬浮组件如状态栏、背包面板\n  - `css` — 全局样式表（追加式），注入沙盒 iframe <head>\n\n🚨 对于 bootstrap_js / scene_js / ui_js / css 类型，content 务必使用 {{path}} 占位符引用 sandbox/ 目录下的 .js/.css 文件，例如 content: \"{{sandbox/statusbar.js}}\"。不要将代码直接写为 content 的值——内联代码会撑爆 output-blocks.yaml，且无法通过 Edit 工具单独修改。正确流程：先 Write 创建 sandbox/xxx.js，再 Output 引用它。后续修改代码时直接 Edit 对应的 .js/.css 文件，系统每 3 秒自动检测引用文件变化、重新渲染并 SSE 推送到前端，无需再次调用 Output。\n\n🚨 使用 rich_text 类型前，必须先调用 SkillRead(name=\"teahouse-sandbox-richtext-render\") 加载 BBCode 标签白名单。BBCode 渲染器只支持 18 个标签（b/i/u/s/color/size/shake/fade/bounce/wave/rainbow/glow/pulse/typing/shadow/highlight/spoiler/tip），使用列表之外的任何标签都不会被渲染，会原样显示为纯文本。\n\nmode 说明：\n- `append`：创建新输出块，后端返回 uuid。前端可据此维护 DOM 节点\n- `replace`：替换已有块的内容，uuid 不变。前端应原地更新而非创建新节点\n- `delete`：移除已有块\n\n当前活跃的输出块列表已注入系统提示词，可参考其 uuid 和 label 进行操作。",
    "parameters": {
      "type": "object",
      "properties": {
        "content": {"type": "string"},
        "label": {"type": "string"},
        "note": {"type": "string"},
        "mode": {"type": "string", "enum": ["append", "replace", "delete"]},
        "target_uuid": {"type": "string"},
        "content_type": {"type": "string", "enum": ["bootstrap_js", "scene_js", "ui_js", "css", "rich_text", "text"]}
      },
      "required": ["content", "label", "note", "mode"]
    }
  }
]
