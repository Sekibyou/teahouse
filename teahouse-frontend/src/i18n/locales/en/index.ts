/**
 * English resource dictionary.
 */
import { enCommon } from "./common"
import { enSettings } from "./settings"
import { enWorkspace } from "./workspace"
import { enChat } from "./chat"
import { enGit } from "./git"
import { enSession } from "./session"
import { enLogin } from "./login"
import { enPlugin } from "./plugin"
import { enMisc } from "./misc"

export const resourceEn = {
  common: enCommon,
  settings: enSettings,
  workspace: enWorkspace,
  chat: enChat,
  git: enGit,
  session: enSession,
  login: enLogin,
  plugin: enPlugin,
  misc: enMisc,
}
